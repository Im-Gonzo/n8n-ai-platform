apiVersion: v1
kind: Secret
metadata:
  name: n8n-secret
  namespace: n8n
type: Opaque
stringData:
  # Generate a random key or get it from your existing n8n instance
  # You can use: openssl rand -base64 24
  N8N_ENCRYPTION_KEY: "your-encryption-key"
